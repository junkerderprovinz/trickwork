{
  "name": "trickwork-ui",
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "e2e": "playwright test -c e2e/playwright.config.ts"
  },
  "dependencies": {
    "noto-color-emoji-flags": "^1.0.0",
    "trickwork-core": "*"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "canvas": "^3.2.3",
    "typescript": "^7.0.0",
    "vite": "^8.0.0",
    "vitest": "^5.0.0"
  }
}
